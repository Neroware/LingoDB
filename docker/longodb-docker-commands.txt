apt-get update

apt-get upgrade

apt-get install git ssh vim gdb software-properties-common

git clone git@github.com:Neroware/LingoDB.git lingodb

apt-get install cmake ninja-build flex bison

apt-get install python3-pip python3-venv

python3 -m venv .venv
source .venv/bin/activate
pip install lit

wget https://apt.llvm.org/llvm.sh
chmod +x llvm.sh
sudo ./llvm.sh 20
apt update
apt install -y \
  clang-20 \
  llvm-20 \
  libclang-20-dev \
  llvm-20-dev \
  libmlir-20-dev \
  mlir-20-tools \
  clang-tidy-20

wget https://apache.jfrog.io/artifactory/arrow/$(lsb_release --id --short | tr 'A-Z' 'a-z')/apache-arrow-apt-source-latest-$(lsb_release --codename --short).deb
apt install -y -V ./apache-arrow-apt-source-latest-$(lsb_release --codename  --short).deb
apt-get update
apt-get install libarrow-dev=21.* libarrow-compute-dev=21.* libparquet-dev=21.*
apt-get install libboost-context1.83-dev

pip install conan
conan remote add dice-group https://conan.dice-research.org/artifactory/api/conan/tentris
git clone https://github.com/tentris/rdf4cpp.git
cd rdf4cpp
git checkout v0.1.13
wget https://github.com/conan-io/cmake-conan/raw/develop2/conan_provider.cmake -O conan_provider.cmake # download conan provider
cmake -B build_dir -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES=conan_provider.cmake # configure and generate
cmake --build build_dir -j8 # compile
cd build_dir/
make install
apt-get install catch2
